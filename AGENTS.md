# image-board

- NO CSS, NO styling, NO animations, NO classes, NO id attributes — everything must be bare HTML with zero style attributes or style tags.
- No CSS frameworks, no Tailwind, no style blocks, no inline styles.
- Pure unstyled HTML only.
